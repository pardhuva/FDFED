import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <h1>Mini Story Builder</h1>
            <p>Create your own short stories by adding characters, settings, and plot points!</p>
        </header>
    );
};

export default Header;